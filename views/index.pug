extends layout

block content
  h1.title ベースフード管理システム
  div.head
    form(action="/products/add-stocks", method="get") 
      button(type="submit") 取得
      p ※「取得」は月に一度、商品が届きたときに押して下さい。
      p &nbsp; &nbsp; 公式マイページから当月買った個数を取得します。
  div.form-result
    form(action="/products/update-stock" method="POST")
      input(type="text" name="input")#input
      button(type="submit") 追加
      p#result 選択した商品: #[span.product-name]
  div#container
  div.contents
    ul 
      if (products)
        each product in products 
          li #{product.name}: 残り #{product.stock}
  script(src="javascripts/barcode.js")
  
  